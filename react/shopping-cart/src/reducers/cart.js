const initialState = {
    addedIds: [],
    quantityById: {}
}
const cart = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}

export default cart